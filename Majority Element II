from collections import Counter

class Solution(object):
    def majorityElement(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        countOfFreq = Counter(nums)
        res = []
        n = len(nums)

        for i in countOfFreq:
            if countOfFreq[i] > n / 3:
                res.append(i)

        return res         
        
